const theme = {
  palette: {
    bg: '#3d5a80',
    fg: '#fff',
    fgAlternate: '#3d5a80',
    accents: {
      green: '#60d394',
      yellow: '#e9c46a',
      orange: '#f4a261',
      red: '#e76f51',
    }
  }
}

export type Theme = typeof theme

export default theme;
