const theme = {
  palette: {
    bg: "#fff",
    fg: "#666",
    headingColor: "#241c15",
    fgAlternate: "#3d5a80",
    headers: {
      lime: "#d8eacc",
      lavender: "#fbf5fe",
    },
  },
};

export type Theme = typeof theme;

export default theme;
